// os script por padrão, fica antes de fechar o body, por se não carrregar p html e css não faz sentido carregar o JS
function enviarContato() {
    let name = document.getElementById('nome')
    /*
    Criado uma variavel nome que vai receber o conteudo do id='nome'
    get= Pega, Element= Elemento, by=por, Id=nome, ('')=nome da id do HTML
    alert(nome.value)
    */
   
   }